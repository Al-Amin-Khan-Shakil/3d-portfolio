import React from "react";
import {Tilt} from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.7, 2)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        I'm a full-stack developer with hands-on experience building and maintaining responsive user interfaces and scalable back-end systems. Over the years, I've worked with Ruby, JavaScript, TypeScript, Ruby on Rails, Node.js, React, Three.js, Tailwind, and PostgreSQL, creating solutions that balance performance and user experience. I’m comfortable working with RESTful APIs and follow the MVC pattern in most projects. I value clean, efficient code and incorporate tools like RSpec and Jest to ensure quality. With experience in Git and AWS, I thrive in remote teams—leading code reviews, mentoring peers, and solving complex problems quickly. I’m a fast learner who enjoys collaborating closely with clients to bring ideas to life.
      </motion.p>

      <div className='mt-20 flex flex-wrap justify-center 2xl:justify-start gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");